# console print console information

The console.log method is used to output information on the console. It can accept one or more parameters, concatenate them and output.

## Example

console.log console print, sample code, the following code can be used in any JS file: `console.log("foo and bar!");`

<Div style={{ display: "flex", justifyContent: "right" }}>

   <Link type="next" title="Log" link="Manual/JS API/Log"></Link>
</Div>
